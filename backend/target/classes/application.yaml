spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: MySQL8


  # em produção: configurar o SSL/TLS corretamente entre sua aplicação e o banco de dados
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/carteirafiisbase?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root #criar um .env para variável de ambiente depois
    password: ceobigin123 #criar um .env para variável de ambiente depois

  flyway:
    enabled: true
    locations: classpath:db/migration